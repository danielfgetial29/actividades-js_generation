const arr = [];

// ¡Escribe tu código debajo de esta línea!
//uso for para llenar el array, declaro 1 en 1 que es donde empieza
// luego la condicion para que se detenga que es que i debe ser menor a 21
// y el contador o el incremento para que aumente de 1 en 1,
// y en cada recorrido va guardando el incremento con push()

for (let i = 1; i < 21; i++) {
  arr.push(i);
}
// ¡Escribe tu código encima de esta línea!

arr.forEach((element) => console.log(element));
// console.log(arr);
